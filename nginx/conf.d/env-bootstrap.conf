# Committed bootstrap snippet so `include /etc/nginx/conf.d/env-*.conf` always matches ≥1 file.
# Real environment routes are written as `${ENV_ID}.conf` (see platform/create_env.sh).
location = /__sandbox_nginx_ok {
    access_log off;
    return 204;
}
